services:
  - type: web
    name: flashread
    runtime: node
    plan: free
    repo: https://github.com/will-king123/flashread
    branch: main
    buildCommand: npm ci --omit=optional
    startCommand: npm start
    autoDeployTrigger: commit
